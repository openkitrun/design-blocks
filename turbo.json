{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "outputs": [],
      "inputs": ["**/*.{js,ts}"]
    },
    "lint:types": {
      "dependsOn": ["lint"],
      "inputs": ["**/*.{js,ts}"]
    },
    "prettier": {
      "outputs": [],
      "inputs": ["**/*.{js,ts}"]
    },
    "bundlesize": {
      "dependsOn": ["^build", "lint"]
    },
    "clean": {
      "cache": false
    }
  }
}
